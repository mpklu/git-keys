# API Tokens for git-keys
# Place this file as .env in your git-keys project directory
# 
# Format:
#   GITHUB_API_TOKEN_[account] = your_github_token
#   GITLAB_TOKEN_[account] = your_gitlab_token
#
# The [account] must match the account name in your ~/.git-keys.yaml

# Example for GitHub accounts
GITHUB_API_TOKEN_myusername=ghp_your_github_personal_access_token
GITHUB_API_TOKEN_workuser=ghp_another_github_token

# Example for GitLab accounts
GITLAB_TOKEN_myusername=glpat-your_gitlab_personal_access_token
GITLAB_TOKEN_workuser=glpat-another_gitlab_token

# How to get tokens:
#
# GitHub:
#   1. Go to https://github.com/settings/tokens
#   2. Click "Generate new token (classic)"
#   3. Give it a name like "git-keys"
#   4. Select scopes: admin:public_key (or write:public_key)
#   5. Generate and copy token
#
# GitLab:
#   1. Go to https://gitlab.com/-/profile/personal_access_tokens (or your self-hosted URL)
#   2. Create a personal access token
#   3. Name: "git-keys"
#   4. Scopes: api
#   5. Create and copy token
